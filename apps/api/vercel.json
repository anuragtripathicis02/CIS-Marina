{
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@yacht-platform/web && rm -rf apps/api/.next && cp -R apps/web/.next apps/api/.next",
  "outputDirectory": ".next"
}
